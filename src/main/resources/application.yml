
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-security-jpa
    username: root
    password: root


  jpa:
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect